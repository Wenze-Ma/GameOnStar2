import './loading.css';

const Loading = () => {
    return (
        <div className='loading-container'>
            <span>GAME ON STAR</span>
            <span>GAME ON STAR</span>
        </div>
    );
};

export default Loading;
